# MatrixTrade (trading dashboard, /trades, /connect)
MATRIXTRADE_PASSWORD=change-me-trading

# ARGUS (/argus — separate login, professional journal)
ARGUS_PASSWORD=change-me-argus

# PIN for private entries (optional second lock)
ARGUS_PRIVATE_PIN=change-me-private-pin

# Legacy env names still work during migration:
# HEALTH_VAULT_PASSWORD, HEALTH_VAULT_SECRET
