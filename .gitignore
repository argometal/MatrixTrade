# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# portable Node (copy locally — see runtime/README.md)
/runtime/node/

# next.js
/.next/
/out/

# misc
.DS_Store
*.pem
npm-debug.log*
.env*.local

/argus-email-bridge/node_modules/
/argus-email-bridge/cloudflared/*.json
/argus-email-bridge/cloudflared/*.token
/bridge/.wrangler/
/bridge/.dev.vars

# typescript
next-env.d.ts

# Obsidian trade notes (your data — keep local)
/vault/Trades/*.md
*.matrixtrade.tmp
Chaos.bundle

# ARGUS — private journal data (never commit)
/data/argus/
/data/trading-inbox.json
/data/health-vault/

# ArgusForge — registry index + chaos staging metadata (not product truth)
/data/forge/

# Trading live data (seed to Supabase — see md/integrations/supabase-cloud-first.md)
/data/trades.json
/data/ai-notes.json

# OS / editor
Thumbs.db
*.swp

# Chaos — separate repo; ignore only nested .git (sources sync via MatrixTrade pull)
/tools/Chaos/.git/
.vercel
