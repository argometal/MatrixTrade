# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# portable Node (copy locally — see runtime/README.md)
/runtime/node/

# next.js
/.next/
/out/

# misc
.DS_Store
*.pem
npm-debug.log*
.env*.local

# Cloudflare bridge (local only)
/bridge/node_modules/
/bridge/.wrangler/
/bridge/.dev.vars

# typescript
next-env.d.ts

# Obsidian trade notes (your data — keep local)
/vault/Trades/*.md
*.matrixtrade.tmp

# ARGUS — private journal data (never commit)
/data/argus/
/data/trading-inbox.json
/data/health-vault/

# OS / editor
Thumbs.db
*.swp
