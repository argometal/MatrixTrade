/** Lightweight Control Library Index — labels only; AI requests one copy row next. */

export function buildLibraryIndexBrief(): string {
  return [
    "=== LIBRARY INDEX ===",
    "Copy from Control → Library. Nav rows open drawers — they are NOT copy buttons.",
    "Control home has exactly 4 primaries: Start Here · Stock Files · Library · Apply.",
    "After this index, request ONE exact visible copy-row label — not a nav name.",
    "",
    "DISCOVERY CHAIN (existence → location → copy target → fallback)",
    "1. Does a Stock File exist for this ticker?",
    "   → Control → Stock Files (pick list / search). Do NOT assume existence or invent ST-/PLAN- ids.",
    "   → If ticker window already open: Analyze with AI (preferred — includes Mechanics + dossier).",
    "2. Need full operating rules? → Library → Mechanics → copy row 'MTA Mechanics'.",
    "   (Start Here is enough to begin routing — do not require Mechanics to start.)",
    "3. Need MTAE procedure only? → Library → Technical Analysis → copy row 'MTAE protocol'.",
    "4. Need Scout universe data? → Library → Scout Desk → 'Scout desk overview'.",
    "5. Need Apply write contract? → Control → Apply → 'Apply schema contract' (not Library).",
    "6. Library Index itself lists CATEGORIES only — not ST-/PLAN- inventories.",
    "FORBIDDEN: inventing UI routes (e.g. Control → MTAE) or assuming a Stock File exists without confirmation.",
    "",
    "RULES vs DATA",
    "- Operating rules (MAF, Entry Solver, R$, TF governance, EXECUTABLE PLAN) → Library → Mechanics → MTA Mechanics.",
    "- Do NOT ask for separate MAF protocol or Entry Solver copies.",
    "- Library rows below are specialized procedure / data — Start Here owns first-contact routing.",
    "",
    "Mechanics (Library nav)",
    "- Copy row: MTA Mechanics",
    "- Full constitution — request only when depth is required",
    "",
    "Technical Analysis (Library nav)",
    "- Copy row: MTAE protocol",
    "- MTAE: structure, zones, targets, invalidation, participation",
    "- No capital decision, no Entry Solver, no Scout verdict",
    "",
    "Playbook (Library nav)",
    "- Copy row: Playbook snapshot",
    "- Reusable methods, entry families (A rebound vs B trend continuation), checklists, experiments",
    "",
    "Scout Desk (Library nav)",
    "- Copy row: Scout desk overview",
    "- Decision, entry, stop, target, R, opportunity quality, capital gate",
    "",
    "MAF (Library nav)",
    "- No separate protocol copy row — MAF rules are inside MTA Mechanics",
    "- Drawer reserved for future MAF experiment/data snapshots only",
    "- FORBIDDEN: do not ask the human to copy Learning — that nav label is retired",
    "",
    "Also available (primary / windows — not Library drawers):",
    "- Start Here — intent/UI router (Control → Start Here)",
    "- Apply schema contract — Control → Apply (Apply Mode only)",
    "- Analyze with AI — Stock File window CTA (preferred ticker loop; includes full Mechanics)",
    "- Apply — paste Validate Accept (write path)",
    "- Dashboard snapshot — Dashboard window menu",
    "- {TICKER} · {ID} historical attribution evidence — historical/planless closed trade detail (not accepted MAF)",
    "",
    "=== REQUEST ===",
    "Ask the human to copy the exact visible copy-row label you need next.",
  ].join("\n");
}
